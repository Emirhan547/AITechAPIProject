@using AITech.WebUI.DTOs.AboutItems
@model List<ResultAboutItemDto>
@{
    ViewData["Title"] = "About Items";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    int count = 0;
}

<div class="card card-outline card-primary">
    <div class="card-header">
        <h3 class="card-title">
            <i class="fas fa-list-ul mr-1"></i> Hakkımızda Maddeleri
        </h3>
        <div class="card-tools">
            <a href="/Admin/AboutItem/CreateAboutItem" class="btn btn-primary btn-sm">
                <i class="fas fa-plus"></i> Yeni Madde
            </a>
        </div>
    </div>

    <div class="card-body p-0">
        <table class="table table-bordered table-hover mb-0">
            <thead class="thead-light">
                <tr>
                    <th style="width:50px">#</th>
                    <th>Başlık</th>
                    <th style="width:160px">İşlemler</th>
                </tr>
            </thead>
            <tbody>
                @if (Model != null && Model.Any())
                {
                    foreach (var item in Model)
                    {
                        count++;
                        <tr>
                            <td>@count</td>
                            <td>@item.Title</td>
                            <td>
                                <a href="/Admin/AboutItem/UpdateAboutItem/@item.Id"
                                   class="btn btn-info btn-sm">
                                    <i class="fas fa-edit"></i>
                                </a>
                                <a href="/Admin/AboutItem/DeleteAboutItem/@item.Id"
                                   class="btn btn-danger btn-sm"
                                   onclick="return confirm('Bu madde silinsin mi?')">
                                    <i class="fas fa-trash"></i>
                                </a>
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="3" class="text-center text-muted">
                            Kayıt bulunamadı
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
